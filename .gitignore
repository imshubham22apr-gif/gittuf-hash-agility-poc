# Keys and runtime dirs — never commit
keys/
work/
archives/

# Loose SSH key files generated during PoC runs — never commit private keys
root
root.pub
dev
dev.pub

# Throwaway test/demo repos created during local PoC exploration
old-repo/
test-clean-repo/
demo-repo/
fresh-repo/
test-sync-repo/

# Environment and OS artifacts
*.exe
*.test
.DS_Store
Thumbs.db
gittuf-upstream/

dist/*
*.prof
vendor*
